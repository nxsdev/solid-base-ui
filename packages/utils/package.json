{
  "name": "@solid-base-ui/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./create-required-context": "./src/create-required-context.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "oxlint src",
    "lint:fix": "oxlint --fix src",
    "format": "oxfmt --check src",
    "format:write": "oxfmt src",
    "test": "vitest run --config vitest.config.mts"
  },
  "dependencies": {
    "solid-js": "2.0.0-beta.1"
  }
}
