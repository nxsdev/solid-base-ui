export enum PopoverTriggerDataAttributes {
  /**
   * Present when the corresponding popover is open.
   */
  popupOpen = "data-popup-open",
  /**
   * Present when the trigger was used by press interaction.
   */
  pressed = "data-pressed",
}
